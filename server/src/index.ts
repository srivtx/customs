/**
 * Customs gate server — entry point.
 * Lands Day 1 night: HTTP adapters, mandate signing/verification, audit chain.
 * The contract it enforces is already locked: server/src/mandate/types.ts
 */
console.log("customs gate server — scaffold. Contract: server/src/mandate/types.ts");
