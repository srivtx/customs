# apps/storefront

The buyer screen: chat intent → cart → bounded mandate → agent pays (Razorpay test-mode).

Next.js (App Router) + Tailwind. Scaffolds Day 1 PM. The phone-frame chat, the mandate
approval card, the mandate status strip, and the attack-me input live here. Talks to the
gate server only — never to Razorpay directly.
