# apps/merchant

The Customs desk: traffic by protocol & trust tier, the live channel P&L meter
(agent GMV vs AI serving cost, at-1M projection), blocks with reason codes, and the
span-by-span replay timeline (sortie lineage).

Next.js (App Router) + Tailwind. Scaffolds Day 1 PM. Reads gate events and OTel spans.
